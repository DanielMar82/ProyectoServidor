<!DOCTYPE html>
<html>
    <head>
        <!-- ESTILOS -->
        {% block stylesheets %}
            <link href="{{ asset('index.css') }}" type="text/css" rel="stylesheet" />
        {% endblock %}
        <title>{% block title %}Pág Registro o Login{% endblock %}</title>
        <!-- Fuentes/Google fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Rubik+Dirt&family=Rubik:wght@300&display=swap" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css" rel="stylesheet">
        <!-- Bootstrap -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    </head>
    <body class="body">
        {% block body %}

        {# MENU #}
        {% include 'menu.html.twig' %}

        <!-- CAMINO DE MIGAS -->
        <div class="caminoMigas">
            <ol>
              <li class="migasAnt"><a href="{{ path('index') }}">Página principal</a></li>
              <p>/</p>
              <li class="migasAct">Inicio Sesión - Crear cuenta</li>
            </ol>
        </div>

        <section class="seccionLogin">
            <div class="divImagenForm">
                <div class="textoBienvenida">
                    <h1>¡Bienvenido!</h1>
                    <p>Regístrate ahora y accede a una amplia variedad de recursos diseñados para músicos de todos los niveles y estilos. Explora nuestra biblioteca de partituras y tablaturas para guitarra acústica, guitarra eléctrica, bajo y ukelele, aprende a tocar tus canciones favoritas con nuestros detallados video tutoriales y perfecciona tus habilidades musicales con nuestros video cursos especializados. Además, descubre nuestra tienda online donde podrás adquirir los mejores instrumentos de cuerda, accesorios y más. Al registrarte, disfrutarás de funcionalidades como poder guardar las canciones y los vídeos en tu perfil y también podrás comprar y escribir comentarios.</p>    
                </div>
            </div>

            <div class="divFormularios">
                <img src="../imagenes/Logotipo.png" alt="Icono Web temporal" class="logo">

                <h1 class="formTitulo">INICIAR SESIÓN</h1>

                {% if error %}
                    <div>{{error.messageKey|trans(error.messageData,'security')}}</div>
                {% endif %}

                <form class="formulario" action={{ path('app_login') }} method="post">
                    <label for="username">Correo: <input type="text" name="_username" value="{{ last_username }}"></label>
                    <br />
                    <br />
                    <label for="password">Contraseña: <input type="password" name="_password"></label>
                    <br />
                    <br />
                    <input type="submit" value="Ingresar" class="botonForm" name="input boton iniciar sesion">
                </form>

                <hr>
                <h1 class="formTitulo">REGISTRARSE</h1>
                {{ form_errors(RegistroForm) }}

                {{ form_start(RegistroForm, {'attr': {'class': 'formulario'}}) }}
                    {{ form_row(RegistroForm.nombre, {
                        label: 'Nombre:'
                    }) }}
                    {{ form_row(RegistroForm.email, {
                        label: 'Correo:'
                    }) }}
                    {{ form_row(RegistroForm.plainPassword, {
                        label: 'Contraseña:'
                    }) }}
                    {{ form_row(RegistroForm.aceptarTerminos) }}
                    <button type="submit" class="botonForm">Registrarse</button>
                {{ form_end(RegistroForm) }}
            </div>
        </section>

        {# PIE DE PÁGINA #}
        {% include 'piePagina.html.twig' %}

        {% endblock %}

        {# INCLUDE JS #}
        <script src="{{ asset('include-html.js')}}"></script>
    </body>
</html>